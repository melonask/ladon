---
layout: home
hero:
  name: Ladon
  text: Configured address pools for EVM, Bitcoin, and Solana
  actions:
    - theme: brand
      text: Get started
      link: /getting-started/
---

Ladon validates a universal TOML configuration and keeps pre-derived addresses available in a SQLite or PostgreSQL store. Its public CLI has only `check` and `pool`.
